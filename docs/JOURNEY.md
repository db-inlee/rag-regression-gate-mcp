# JOURNEY — 검증으로 실제 잡은 결함들

> 이 프로젝트의 핵심 가치는 "동작하는 코드"가 아니라 **"검증으로 결함을 잡고 정직하게 고친 과정"**이다.
> 각 단계마다 가설을 세우고 → 실제 데이터/로그로 교차검증 → 결함을 찾아 수정했다. 아래는 happy-path가
> 아니라 **검증이 없었으면 그대로 새어나갔을** 결함과 그 수정의 기록이다. (데모를 위한 결과 조작은 없음.)

---

## 설계 결정 — 채점 전략: judge를 *선택적으로*, 그리고 *검증해서* 쓴 이유

우리는 judge(LLM 채점)를 **아예 안 쓴 게 아니라**, 슬라이스별로 **결정적 채점을 기본으로 두고 judge는 꼭 필요한 곳에만** 썼다. 그리고 그 judge조차 gold로 검증한 뒤에 신뢰했다.

| 슬라이스 | 채점 방식 | judge | 근거(코드) |
|---|---|---|---|
| 숫자추론·표값 | **결정적** — 한국어 단위 정규화(조=1e12·억=1e8…) + **상대 허용오차 ±0.1%** | ✗ | `app/evaluator/scorer.py` (`_UNIT`, `tol = 0.001*abs(gold)`) |
| 본문(서술형) | **judge 사용** — gold key-point 대비 LLM 판정 | ✓ (gold로 검증) | `app/evaluator/judge.py` + `validate_judge.py` |
| 답없음(no_answer) | **결정적** — 거부 문구 매칭 | ✗ | `scorer.is_refusal` (`_REFUSAL`) |
| retrieval_miss | **결정적** — gold 근거 ⊆ 검색 청크 집합 비교 | ✗ | `attribution.is_retrieval_miss` (`gold_retrieved`) |

**이유 1 — 재현성(회귀 게이트의 생명):** 회귀 게이트는 *'같은 입력엔 같은 판정'* 이 전제다. judge 의존이 크면 비결정성이 들어와 거짓경보가 생긴다. 그래서 채점·생성 모두 `temperature=0`+seed 고정으로 돌렸고, 그 결과 **노이즈밴드 std=0**(5회 재실행 분산 0)을 달성했다. judge는 본문 슬라이스에만 국한해 비결정성의 표면적을 최소화했다.

**이유 2 — judge도 검증 대상:** 본문 채점에 쓴 gpt-4o judge를 정답/오답 probe로 검증해 **judge_accuracy 0.987**(혼동행렬 포함)을 확인했다. 처음엔 무효 probe(맞는 내용을 덧붙인 변형)로 0.933까지 deflation 됐던 것을, neg_subtle을 '진짜 사실 어긋남'으로만 재구성해 **0.933 → 0.987**로 고쳤다. → [`reports/judge_validation.json`](../reports/judge_validation.json), 상세는 아래 Phase 2.

**RAGAS 대비 — 부정이 아니라 목적의 차이:** RAGAS는 훌륭한 범용 RAG 평가 프레임워크다. 우리는 그것을 부정하는 게 아니라, *'CI에서 도는 회귀 게이트'* 라는 목적상 **결정성을 최우선**으로 뒀을 뿐이다. *개념은 빌리되(groundedness·answer correctness 등의 발상), 측정은 가능한 한 결정적으로* 한 것이 핵심 차이다 — judge 한 번의 흔들림이 PASS/FAIL을 뒤집으면 게이트로서 못 쓰기 때문. 실제로 judge를 본문 밖으로까지 넓게 쓰면 위에서 검증한 **'거짓경보 0건'**(같은 config 재실행 → 항상 PASS, Phase 3)이 원리적으로 불가능해진다.

---

## 설계 결정 — 정직한 경계: gold(평가셋)가 전제이고, reference-free는 범위 밖

과장하지 않기 위해, 이 도구가 **무엇을 못 하는지**도 설계 근거로 적는다. 이 엔진은 *'평가셋(질문+정답)을 가진 RAG'* 에 범용이며, **gold가 전제**다. gold가 얼마나 갖춰졌느냐에 따라 작동 범위가 셋으로 갈린다:

| 가진 gold | 작동 범위 | retrieval_miss / groundedness 처리 |
|---|---|---|
| **정답 + 근거 라벨** (우리 DART) | accuracy · retrieval_miss · groundedness **전 기능** | gold 근거 ⊆ retrieved 집합 비교(결정적) |
| **정답만** (근거 라벨 없음 — 더 흔한 경우) | accuracy 작동 | retrieval_miss는 *'정답 텍스트가 검색 청크에 등장하나'* 로 대체 가능 — 실제로 위키 어댑터 `wiki_value_present`가 정답 문자열↔청크 매칭으로 이 방식을 쓴다 |
| **정답조차 없음** (reference-free) | **범위 밖** | 정답 없이 옳고 그름을 판정하려면 judge 의존이 불가피 → 위에서 세운 *결정성* 원칙과 충돌하므로 다루지 않는다 |

**핵심 논리:** 회귀 감지는 본질적으로 **'기준'** 이 있어야 성립한다. 고정된 비교 기준(평가셋) 없이 *'깨졌다'* 를 판단하는 것은 원리적으로 불가능하다. 그래서 **평가셋 전제는 이 도구의 한계라기보다 '회귀 게이트'라는 정의에 내재한 조건**이다. 테스트셋 기반 평가 도구(promptfoo 등)와 RAGAS의 reference 기반 메트릭도 같은 전제를 공유한다.

---

## Phase 1 — 표추출 / 인덱싱

| 검증으로 발견한 결함 | 근거 | 수정 |
|---|---|---|
| **표에 연도가 없음** — 컬럼이 `제55기/제54기`인데 연도 매핑은 표 밖 텍스트에만 | 손익계산서 표 샘플 검증 | 표 위 `제 N 기 YYYY.01.01 부터` 파싱 → 헤더에 `제55기(2023)` 병기 (wrong_period 방지) |
| **단위 멀티표기 잘림** — `(단위: 억원, %)` → `(단위: 억원,)` | grep으로 5건 발견 | 정규식을 닫는 괄호까지 그리디 캡처로 |
| **표 본문 이중 출력** — 같은 표가 raw 텍스트 + 마크다운으로 중복 | 손익계산서 값 등장 횟수 확인 | bbox로 표 영역 제외, 마크다운 표만 |
| **섹션 라벨이 연도헤더에 혼입** — 재무상태표 `제55기(2023) / 자산` | grep `기(YYYY) / 자산` 3사×2건 | 첫 컬럼만 채워진 행은 헤더 아닌 본문 구분행으로 분리 → 잔존 0건 |
| **큰 표 헤더 반복 미검증 위험** | "짧은 표만 검증 말라" 지적 → 분할표로 검증 | chunk_size 초과 표는 행분할+연도헤더 반복; 분할표 69개 전수 헤더 동일 확인 |

## Phase 1.2 — 평가셋 100문항

| 검증으로 발견한 결함 | 근거 | 수정 |
|---|---|---|
| **본문 답이 환각/“없음”/순환** | case_082("…없습니다"), case_087(순환 비-사실) | 폐기 후 본문에 **문자그대로 실재**하는 사실로 재생성 + 자체점검 |
| **가짜 답없음 트랩(미검증 가정)** | 탄소배출/배당성향/해외매출/연구개발인력 — 실제 부재 미확인 | 대상 보고서 **grep으로 부재 확인된** "직원 1인당 영업이익/당기순이익"으로 교체 |
| **숫자 질문에 회사명 누락** — "2023년 연결 당기순이익은?"이 3사 모호 | baseline 파이프라인 교차검증에서 발견(엉뚱한 회사 검색) | table_value·numeric 60문항에 `{회사} {연도}년 보고서 기준,` 접두사 |
| **no_answer 형식 비통일** — "이 보고서에서…" | 슬라이스 일관성 점검 | `{회사}{연도}년 보고서 기준,`로 통일, 미래연도 트랩은 보고서연도<질문연도 유지(assert) |
| **macOS NFD/NFC 유니코드** — 파일명·한글 키 매칭 실패 | comparison 0건/KeyError로 발현 | 전 경로 NFC 정규화 |
| **하이닉스 단일 포괄손익계산서 형식** — 매출/영업이익 누락 | 회사별 라벨 probe | `포괄손익계산서`도 income으로(문서순서+dedup) |

## Phase 2 — 채점 / judge / 귀인

| 검증으로 발견한 결함 | 근거 | 수정 |
|---|---|---|
| **채점 false negative** — 모델 "14조 2,396억원"(=14,239,600) vs gold 14,239,592를 오답 처리 | **case_034** baseline 교차검증 | table_value 정확일치 → **상대 허용오차 ±0.1%** (case_031 0.14%·case_002 0.8%는 여전히 incorrect) |
| **judge 무효 probe로 정확도 deflation** — '맞는 내용 덧붙인' 변형이 여전히 정답이라 거짓 FP | **case_082·089**, judge_accuracy 0.933 | neg_subtle을 '진짜 사실 어긋남'으로만 + 생성후 자체 모순점검 → **0.933 → 0.987**(잔존 FP는 case_093 용어교체 1건) |
| **암기/운 정답이 RAG 점수 부풀림** — 맞았지만 정답값이 검색근거에 없음 | **case_034** | `grounded_correct` vs `unsupported_correct` 분리, answerable_accuracy 헤드라인은 grounded 기준 |
| **가짜 retrieval_miss** — gold 표는 strict-miss지만 동등표(요약재무)에 정답값 존재 | **case_038** | `retrieval_success`를 strict(표) + value_present(값 어디든) **이중 정의**로 분리 보고 |
| **파생값(증가율) groundedness 과소** — %는 셀에 없어 무조건 unsupported | case_043/046 | 원천 셀 2개가 검색됐으면 grounded 인정(case_043 grounded, case_046은 미검색 → unsupported 유지) |

## Phase 3 — 회귀 판정 / 게이트

| 검증으로 발견한 결함 | 근거 | 수정 |
|---|---|---|
| **노이즈밴드 0이면 1케이스 플립을 회귀로 오판 위험** | 노이즈밴드 측정 std=0 | 측정값은 정직한 0 유지, **판정에서만 floor `max(밴드, ±1 case)`** |
| **거짓경보 검증 필요** | 같은 config 두 실행(r1 vs r2) | delta 전부 0, CI [0,0] → **PASS(거짓경보 0건)** 확인 |
| **검출기가 무음 no-op일 위험** | 합성 회귀 주입 검증 | 12케이스 강등 시 retrieval_miss CI [+6,+18]로 FAIL → 변별력 확인 |
| **게이트 WARN 문구에 "유의" 오표기** | WARN인데 "유의 하락"으로 출력 | WARN은 "유의" 제거 + "[경계]" 표기 |

## Phase 5 — 메타 평가 (config만 변경)

| 검증으로 발견한 결함/사실 | 근거 | 처리 |
|---|---|---|
| **음수 기준 증가율** — 적자 기준이라 6066% 등 무의미 | **case_052**(하이닉스 매출총이익), **case_044**(영업이익, 같은 결함) | growth는 **기준연도 값>0**만; 둘 다 양수기준으로 교체 |
| **chunk_size 400 회귀 불충분(WARN)** — 가설은 FAIL | A-1 게이트 결과 | **결과 그대로 보고** + top_k 5→1로 강화해 FAIL 확보(정직성) |
| **reranker가 "헛개선"** — 직관과 달리 유의 개선 없음 | B 게이트 WARN | **가설과 다른 결과 그대로 보고** — 게이트가 헛개선 인정 안 함(가드의 존재 이유) |
| **중립(overlap)도 1케이스 경계 흔들림** | C 게이트 WARN | FAIL 오판은 floor가 막음; "완전 PASS 아님"을 정직 기록(floor ±2 트레이드오프 명시) |

## Phase 6 — CI 통합

| 검증으로 발견한 결함 | 근거 | 수정 |
|---|---|---|
| **게이트가 gold(eval_cases)에 의존** — CI 입력 과다 | import/사용 추적 | per-case 판정을 attribution에 enrich → **gold 제거**, `eval_cases.jsonl` 삭제 후에도 PASS/FAIL 동작 증명 |
| **게이트 출력이 reports/ 오염** | reports/는 원본 전용이어야 | 출력 `gate_runs/`(gitignore)로 분리, reports/ 청소 |
| **README 문서 경로 부정확** — `tasks/`·`docs/plan` | 파일 위치 점검 | 실제(루트)로 수정 |

---

## 메타 교훈

- **검증 단위가 잘게 쪼개져 있을수록 결함이 드러난다**: 케이스 단위(case_034/038/052…)로 교차검증했기에 집계 점수에 묻힐 결함을 잡았다.
- **가설과 다른 결과는 결함의 신호이거나 정직성의 기회다**: chunk400 WARN·reranker 헛개선은 꾸미지 않고 그대로 보고했고, 그게 오히려 게이트의 분별력을 증명했다.
- **"검증 도구 자체도 검증한다"**: judge를 gold로 검증(0.987)하고, 게이트를 거짓경보 테스트(0건)로 검증했다 — 측정하는 것이 못 믿을 만하면 측정 전체가 무의미하므로.
